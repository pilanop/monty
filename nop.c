#include "monty.h"

/**
 * nop - Does nothing.
 * @stack: Pointer to a pointer pointing to top node of the stack.
 * @line: Integer representing the line number of of the opcode.
 */
void nop(stack_t **stack, unsigned int line) {
	(void) stack;
	(void) line;
}
